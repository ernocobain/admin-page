import 'package:flutter/material.dart';

class AddPage extends StatelessWidget {
  static const String id = "/add-page";
  const AddPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Add Page");
  }
}
