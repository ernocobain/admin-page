import 'package:flutter/material.dart';

class TrashPage extends StatelessWidget {
  static const String id = "/trash";

  const TrashPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Trash Page");
  }
}
